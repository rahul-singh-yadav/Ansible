---
# defaults file for ansible-role-service-docker-amzlinux2

vars:
  DOCKER_COMPOSE_VERSION: "v2.2.2"
  DOCKER_PACKAGES:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg-agent
    - lsb-release
    - software-properties-common